# Execução da recuperação da POC — 15/09/2026

Responsável pela consolidação: Codex. Objetivo solicitado: entregas e testes até
18h de 15/09; fuso aguardando confirmação. O objetivo não altera critérios de
aceite, não reativa o modo adiado e não autoriza produção.

## Candidato local: integridade documental e checkpoints

- 959 testes gerais passaram; 55 ficaram ignorados nessa execução sem banco.
- A suíte separada de banco descartável passou com 58 testes e zero ignorados.
  Parte dos testes também aparece na suíte geral; não somar contagens.
- TypeScript, ESLint e build passaram.
- Oito cenários específicos de checkpoints passaram no React real com API
  sintética: URL lenta, presença sem jornada, menu, troca de empresa, erro,
  nova tentativa, resposta antiga e celular.
- Oito cenários do build real com banco descartável passaram: acesso anônimo,
  dashboard autenticado, fila/detalhe, política, upload vazio, campo, troca de
  empresa e menu mobile. Sessão sintética; não é teste de senha nem de provedor.

Correções desta rodada: gravação atômica dos campos da nota e da despesa;
rollback quando o log falha; concorrência entre web e WhatsApp com isolamento
entre empresas; XML estrutural e base64 limitado antes de decodificar; dimensão
da imagem para evitar deslocamento tardio da âncora.

## Revisões e pendências

Agentes fizeram revisão técnica de IA e implementaram testes. Isso não equivale
a aprovação humana. O serviço dos agentes interrompeu por falta de créditos;
as alterações ficaram preservadas e a consolidação continua localmente.

Achados adicionais integrados e validados na candidata:

1. Condição textual pendente força revisão e não produz aprovação/negação incondicional.
2. Edição, ativação e auditoria de política usam transação e bloqueios para preservar a versão.
3. A autorização da revisão é revalidada sob bloqueio na transação da decisão.

Régua completa de métricas, precedência hierárquica, documentos reais elegíveis,
ensaios de provedores, revisão humana e E12 seguem abertos. Aceite integral: 0/12.

## Ambiente conferido

Homologação ainda em `7f11af7`, versão `1.14.0-rc.20260915t000927z`.
Às 06:56 UTC: worker desligado; ledger compartilhado com 66 reservas de 100;
orçamento fiscal com 0 de 20 consumidas. Nenhuma chamada paga nesta rodada.
Há quatro itens pendentes de entrada; não reativar fila sem examinar o contexto
e preparar o ensaio autorizado. Nenhum deploy efetuado nesta rodada.

## Evidências internas reproduzíveis

- `tools/poc/checkpoints-browser.mjs` e seu roteiro Markdown.
- `tools/poc/browser-smoke.mjs` com build, banco descartável e bloqueio de rede.
- `tools/poc/validar-sql-local.mjs`, executado a partir do worktree correto.
- `db/despesas.concorrencia.integracao.test.ts` e
  `db/documentos.canais.integracao.test.ts`.
- Relatórios e capturas em `/root/Relatorio/entrega-20260915/`; a captura do build
  real está em `docs/poc/evidencias/browser-5c2de302-c32c-498d-ab85-e7f36b6a00a7/`
  no ambiente de trabalho. Arquivos brutos não são enviados automaticamente ao Git.

## Consolidação desta retomada — 2026-09-15T07:39:58.343292+00:00

Fonte validada: `97180f5fdcf136e24e13eb1eec25d07715119771`. Branch `integrate/poc-candidata-20260915`, enviada
ao GitHub. A `main` foi consultada por fetch e permanece em `7f11af7`.
O estado de homologação acima é o registro das 06:56 UTC, não uma nova consulta.

A integração inicialmente falhou no TypeScript por ausência do rótulo da nova
lacuna e em um teste que esperava exigência incondicional. Ambas foram corrigidas
em `97180f5`; tipos, lint, suíte geral e build passaram na repetição.
Os 58 testes SQL passaram em banco descartável após liberar a conexão local
bloqueada pela sandbox. O primeiro ensaio de checkpoints excedeu o tempo de
inicialização; o reteste sem compilações concorrentes passou nos oito cenários.
O build real também passou em oito cenários, com limpeza concluída.

A alteração dos modos web de `698ccd8` já foi removida em `5a24ece`; o diff final
respeita o adiamento. CI e revisão remotos ainda devem ser conferidos para a
versão final antes de homologar. Nenhum deploy ou chamada paga nesta retomada.

Evidências: `/root/Relatorio/retomada-poc-20260915/manifesto.json`,
`testes.json`, `sql.json`, `checkpoints-reteste/resultado.json` e
`browser-smoke.json`. As contagens de testes não são aditivas.
